---
okf_version: "0.2"
title: gunkata knowledge base
description: Durable findings and references for the gunkata DAG orchestrator.
generated: { by: claude-code/opus-5, at: "2026-09-17T02:02:39Z" }
---

# gunkata knowledge base

This bundle holds what must still be true in a fresh clone, to a reader who was not in the
session that learned it: findings about the tools and platforms gunkata depends on, and
references to outside material the project relies on. Design law lives in `/docs/spec.md`
and `/AGENTS.md`, never here. One concept per file, full frontmatter, sources on every
claim.

It refuses three things. **State** - run counts, in-flight work, versions of things that
change with the next commit - belongs to its authoritative source, not here; a number lives
in a concept only when the number is the finding. **Secrets** of any kind, because git
history is permanent. **Standing orders**, which belong in `/AGENTS.md`: when a decision
produces both a law and a rationale, `AGENTS.md` gets the one-line law and the rationale
lives here.

Conventions: `findings/` holds `Finding`, `references/` holds `Reference`, and the
generator rejects anything else. Deprecate, never delete. The listing
below is generated by `scripts/kb_index.py` - edit concepts and regenerate; pre-commit
enforces `--check`. There is no update log; git history is the log.

Enter through this listing and open only what the task needs.

<!-- generated by scripts/kb_index.py - edit concepts, not this listing -->

# Findings

* [A gate must run inside the pinned dev shell](findings/a-gate-must-run-inside-the-pinned-dev-shell.md) - A hook inherits the invoking shell, so an environment cached before a flake change fails the gate with a bare exit 127; the pre-push hook therefore wraps itself in nix develop.
* [An ACP agent's final message is its message-chunk text after the last tool call](findings/acp-final-message-is-the-text-after-the-last-tool-call.md) - In the ACP stream a turn's text arrives as agent_message_chunk updates interleaved with tool calls - short preambles before each tool call, the answer after the last one - so the final message is the chunk text accumulated since the last tool_call or tool_call_update, not all of the turn's text and not one messageId.
* [acpx loads Claude with project and local settings only](findings/acpx-claude-skips-user-settings-by-default.md) - acpx launches Claude Code with --setting-sources=project,local, so skills and settings placed in the executor HOME's .claude (user scope) are ignored unless ACPX_CLAUDE_INCLUDE_USER_SETTINGS=1 is set.
* [acpx --format json emits the raw ACP JSON-RPC stream](findings/acpx-json-format-is-the-raw-acp-stream.md) - With --format json acpx writes the ACP JSON-RPC traffic to stdout one object per line - session/update notifications for tool calls, usage and thought chunks, the client requests, and their responses, with stopReason and token usage on the prompt response - and --json-strict keeps non-JSON off it.
* [acpx runs the claude and codex adapters through npm exec, so a bare HOME re-installs them every job](findings/acpx-runs-adapters-through-npm-exec.md) - A Nix-packaged acpx 0.17 finds no bundled claude-agent-acp or codex-acp and runs them via npm exec --yes against its pinned range, never via PATH; with a per-job HOME that is a ~400-900 MB install per job, and one shared npm_config_cache turns it into a sub-second no-op reify.
* [The agy ACP server replaces its token link with a file, and otherwise starts bare](findings/agy-acp-server-replaces-its-token-link-and-stays-bare.md) - Antigravity's ACP server keeps its whole profile under $HOME/.gemini, starts bare in an engine-owned HOME with three links, loads skills only from the Gemini home and the cwd itself (no ancestor walk), loaded no workspace rules at all - but its silent token refresh replaces the linked acp_token.json with a regular file, so the engine must delete it after the job.
* [A Claude executor waits on its subagents reliably only in the foreground](findings/claude-executor-waits-on-subagents-in-the-foreground.md) - In a single-prompt acpx run, background subagents' results can be lost when the lead ends its turn, and Monitor is absent because CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC turns off the feature flags that enable it; foreground Agent calls in one message, or TaskOutput with block, are the reliable waits.
* [A Claude login refresh replaces the linked credentials file and rotates the refresh token](findings/claude-login-refresh-replaces-its-link-and-rotates.md) - Claude Code writes .credentials.json by temp file and rename, so an executor's OAuth refresh replaces its link with a file holding a new single-use refresh token and spends the host's; its refresh lock lives in the executor's own .claude, so jobs and the host never serialize - the engine must hand a newer login back to the host.
* [Claude MCP OAuth tokens are keyed by server name plus a hash of its exact config](findings/claude-mcp-oauth-tokens-keyed-by-server-identity.md) - Claude Code stores MCP OAuth tokens in .credentials.json under "<name>|" + the first 16 hex of sha256 over {type,url,headers}, so a server passed on acpx stdin reuses a stored login only when name, exact URL and empty headers match - and linking the file hands the agent every stored token.
* [A Claude subscription login loads the account's claude.ai connectors into a bare executor](findings/claude-subscription-login-loads-claudeai-connectors.md) - A Claude executor authenticated with the subscription OAuth login auto-loads every claude.ai connector on the account (Gmail, Slack, Drive, Calendar, glim...); ENABLE_CLAUDEAI_MCP_SERVERS=false stops it, and an allowedMcpServers list keyed by serverUrl is the selective alternative.
* [A Claude executor inherits CLAUDE.md files and skills from its cwd's ancestors](findings/claude-walks-ancestor-dirs-for-instructions-and-skills.md) - Claude Code walks the ancestors of its cwd for CLAUDE.md and .claude/skills; the skill walk stops at $HOME or a git root, so a work dir nested inside the executor's own HOME is bounded, and CLAUDE_CODE_DISABLE_CLAUDE_MDS=1 stops the unbounded CLAUDE.md walk.
* [A Codex executor needs its own config.toml, full-access mode and the NixOS env marker to start bare](findings/codex-needs-a-config-file-and-full-access-to-start-bare.md) - Under acpx, Codex with only auth.json in its HOME still loads subscription connectors and plugins, bundled system skills, a cwd AGENTS.md, repo-walk skills and host-keyring MCP tokens, hides late MCP servers, sandboxes the network off and rebuilds PATH through NixOS zsh init; a per-job config.toml, INITIAL_AGENT_MODE=agent-full-access and inheriting __NIXOS_SET_ENVIRONMENT_DONE fix all of it.
* [TMPDIR does not keep an executor out of /tmp - only a mount namespace does](findings/executor-tmp-needs-a-mount-namespace.md) - Agents hardcode /tmp regardless of TMPDIR, so an executor's scratch leaks into the shared host /tmp; an unprivileged user+mount namespace that bind-mounts the job's HOME/tmp over /tmp confines it with stdlib Go and keeps the pid and process group, best effort - hosts without unprivileged user namespaces fall back to the shared /tmp, recorded as private_tmp false.
* [A bare HOME rebuilds Go's build, module and lint caches every job, and all three are safe to share](findings/executors-share-go-build-module-and-lint-caches.md) - With a per-job HOME every executor that checks a Go repo refills GOCACHE, GOMODCACHE and GOLANGCI_LINT_CACHE from scratch; all three are built for concurrent use by processes on one machine, so the engine points them at shared dirs beside the npm cache, cutting a cuttle build-and-lint from 16.4 s to 4.6 s.
* [A review lens needs a top-tier model, and a cheap one fabricates its coverage](findings/review-lens-model-choice-and-fabricated-coverage.md) - On byte-identical inputs and one verbatim cleanliness-lens prompt, claude-opus-5 found 7 of 7 verified findings twice while codex gpt-5.6-sol found 1 (at medium and at high), claude-sonnet-5 found 1 across three runs and agy gemini-3.7-flash-medium found none - and every low scorer but codex shipped a confident "no findings" over a Checked section claiming coverage its executor log refutes.
* [A tool completion can arrive after the agent's final message](findings/tool-completions-can-arrive-after-the-final-message.md) - agy batches its tool_call_update completions at the end of the turn, so the rule that any tool update resets the captured message threw away whole reviews and parked the job with an empty message.md; the engine now resets only on a tool start or a non-terminal update.

# References

* [2026-09-19 report - why a gunkata review missed a bug a session review found (cuttle PR 73)](references/2609-19-cuttle-pr73-review-forensics.md) - Dated forensic report comparing /polish reviews of glim-sh/cuttle#73 run through the gunkata review kata and from a Claude Code session, with the same model, effort, skill and prompt; the kata miss traced to lead prompt anchoring, a checkout mutated mid-review and reasoning variance - not tools, MCP, skills, CLAUDE.md, model or effort - plus the wall-time breakdown, the harness and kata changes that followed, a 12-run blind-scored measurement, and every later setup scored against it: the review-2 DAG and its 2b (clearance audits) and 2c (hand-off fixes) variants, and the single-agent kata with a concision rule, which leads on key items at the lowest cost.
* [Executor models field guide](references/executor-models-field-guide.md) - What each executor family advertises over ACP, what has actually been measured about those models, and the traps that make a wrong choice look like an infrastructure fault - the answer to "which model goes on this node".
* [pond PR 289 as a review ground-truth case](references/pond-pr-289-review-ground-truth.md) - A hand /polish review of tenequm/pond#289 (lance 12 upgrade) with 8 findings and 10 validated non-findings, pinned to head 6a83a53, held as the grading key for a future replay of the gunkata review flow.
